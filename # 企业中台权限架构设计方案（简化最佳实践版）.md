# 企业中台权限架构设计方案（简化最佳实践版）

## 一、结论

```text
角色模板 + 用户直接 ALLOW + 菜单资源包 + 组织岗位任职
```

业务资源码是唯一功能授权原子，例如 `sc.purchase_order.create`。前端、后端、任务和审计共用资源码；不再把 API URL 建成权限项。

## 二、对象

| 对象 | 职责 |
| --- | --- |
| 用户 | 登录主体，可选择多个角色，也可少量直接加授 |
| 角色 | 扁平、可复用的授权模板，没有板块字段 |
| 资源 | 稳定的业务动作权限 |
| 菜单 | 导航入口和资源包载体 |
| 组织 | 企业组织树 |
| 岗位 | 隶属组织，用于设置用户任职 |
| 任职 | 用户 + 组织 + 岗位 + 可选工号 |

## 三、编码

```text
BOARD_INFO
DIR_INFO_CUSTOMER
MENU_INFO_CUSTOMER_LIST
ROLE_SALES_MANAGER
crm.customer.export
```

- 板块：`BOARD_`；
- 目录：`DIR_`；
- 页面菜单：`MENU_`；
- 角色：`ROLE_`；
- 资源：`<domain>.<object>.<action>`，全小写点分段。

编码全局唯一，启用后不改义；资源码不包含 HTTP Method、URL、组件名或表名。

## 四、授权

有效资源：

```text
角色资源 ∪ 角色菜单 CORE
∪ 用户直接资源 ∪ 用户直接菜单 CORE
```

- 只支持 ALLOW；
- 同一资源多来源时去重并保留来源；
- 用户直授原因必填、失效时间选填；
- 常规权限进入角色，直授只处理例外；
- 用户需要更少权限时拆角色，不做反向覆盖。

## 五、菜单包

- 只有页面菜单可授权；
- CORE 随菜单自动生效并在资源页签反显；
- OPTIONAL 显著提示、管理员显式选择；
- HIGH 风险资源不得设为 CORE；
- 父节点勾选保存当时的页面叶子，不包含未来新增页面；
- CORE 实时计算，不复制到用户和角色关系表。

## 六、页面口径

| 页面 | 当前 Demo |
| --- | --- |
| 用户列表 | 不显示内部策略版本 |
| 创建用户 | 姓名、登录账号、状态；无工号和任职 |
| 用户授权 | 角色多选优先展示，再显示菜单、资源和有效权限 |
| 角色 | 名称、`ROLE_` 编码、状态；无板块字段 |
| 组织岗位 | 选择用户、组织、岗位，工号选填 |
| 菜单 | 板块 → 目录 → 页面菜单 |
| 资源 | 名称、业务资源码、风险、状态 |

Demo 不包含数据域、数据范围、客户数据行或权限模拟器交互。

## 七、研发实现

前端统一使用：

```ts
useAuthorization().can('sc.purchase_order.approve')
```

结构性内容优先 `<Can>` 或 `v-if`；`v-permission` 只用于原生 DOM；路由 middleware 只做导航体验。

后端统一使用：

```java
@PreAuthorize("hasAuthority('sc.purchase_order.approve')")
```

每次请求由后端裁决。未认证、资源不存在、资源停用、授权过期或加载失败时默认拒绝。

## 八、存储

建议表：用户、角色、资源、菜单、菜单资源包、用户角色、角色菜单、角色资源、用户菜单直授、用户资源直授、组织、岗位、任职、审计和 outbox。

关键约束：

- 工号在任职表，可空；
- 一期用户只有一个主任职；
- 角色表无板块字段；
- 显式关系入库，CORE 派生结果不入库；
- 授权写入、版本、审计和缓存失效同事务；
- 并发更新使用版本校验。

## 九、二期建议

正式后端可增加“岗位 × 数据域”的数据边界，固定 `NONE`、`SELF`、`DEPT`、`DEPT_AND_DESCENDANTS`、`CUSTOM_DEPTS`、`ALL`。

该能力不在当前 Demo 中。实现时由后端解析并下推 SQL；无任职或无策略默认 NONE；列表、详情、写操作、导出和任务使用同一谓词；前端不做数据过滤。

## 十、检查表

- [ ] 资源码是业务动作，不是 API URL；
- [ ] 编码前缀统一；
- [ ] 角色无板块字段；
- [ ] 创建用户无工号和任职；
- [ ] 任职工号可选；
- [ ] 用户支持多选角色且优先展示；
- [ ] 勾选角色后，角色菜单与 CORE 资源实时反显且不写入用户直授；
- [ ] OPTIONAL 黄色高亮，并区分已由角色满足和待选；
- [ ] HIGH 不进入 CORE；
- [ ] 前端隐藏不是唯一校验；
- [ ] 后端逐请求裁决并默认拒绝；
- [ ] 变更可审计、可解释、可并发控制；
- [ ] Demo 未实现能力没有被写成现状。

> 一句话：角色做复用，直授做例外，菜单做资源包，组织页负责任职，前后端共同校验业务资源码。
