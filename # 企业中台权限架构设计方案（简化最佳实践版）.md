# 企业中台权限架构设计方案（简化最佳实践版）

> 版本：2.0 · 评审修订版  
> 适用：单一小型企业内部中台

## 一、推荐模型

```text
角色模板（常规复用）
  + 用户直接加授（少量 ALLOW 例外）
  + 页面菜单 CORE 资源（自动展开）
  = 用户有效资源

用户有效资源
  AND 岗位在对应数据域的正式范围
  = 最终允许访问
```

原“功能”统一改名为“资源”。资源是唯一授权原子，例如 `sc.purchase_order.create`。前端与后端共同使用同一资源码。

原 REPORT / TEMPLATE “业务资源”层取消。API、服务方法和任务只是执行点，直接声明资源码，不进入运营授权目录。

## 二、核心对象

| 对象 | 作用 | 是否直接授权 |
| --- | --- | --- |
| 用户 | 登录主体；承担角色和少量直接加授 | 授权主体 |
| 角色 | 菜单与资源的可复用模板 | 授给用户 |
| 资源 | 原子业务动作与稳定资源码 | 是 |
| 页面菜单 | 前端入口与 CORE / OPTIONAL 资源包 | 是 |
| 板块 / 目录 | 菜单结构分组 | 否，仅展开当前叶子 |
| 组织 / 岗位 | 用户正式任职与数据边界 | 否 |

## 三、用户与组织岗位分开维护

用户创建只保留：

- 姓名；
- 登录账号；
- 工号；
- 状态。

不在创建用户时设置组织、岗位或初始角色。

组织管理员在“组织与岗位”页面执行：

```text
选择用户 → 选择组织 → 选择岗位 → 保存任职
```

任职变化提升用户策略版本并记录 before / after 审计。

用户授权抽屉只展示菜单、资源和有效权限，不展示数据范围。本期不提供用户临时数据范围。

## 四、菜单资源包

| 类型 | 规则 |
| --- | --- |
| CORE | 页面正常打开所需最小资源，随菜单自动生效并反显 |
| OPTIONAL | 新建、编辑、导出、删除、审批等资源，管理员按需选择 |
| HIGH | 高风险资源不得配置为 CORE |

父节点批量选择只保存当前页面菜单叶子；未来新增菜单不会自动进入历史授权。

## 五、数据范围

数据范围只由“岗位 × 数据域”提供：

```text
NONE
SELF
DEPT
DEPT_AND_DESCENDANTS
CUSTOM_DEPTS
ALL
```

未配置按 NONE。角色决定“能做什么”，岗位决定“能对哪些数据做”。

业务表至少明确：

```text
owner_user_id
owner_org_unit_id
created_by
```

列表、详情、编辑、删除、导出、批量和异步任务必须复用同一数据谓词。

## 六、前后端契约

```ts
// 前端体验控制
hasResource('sc.purchase_order.create')
```

```java
// 后端安全边界
@RequireResource("sc.purchase_order.create")
```

新增 API 无需登记 URL 权限或配置映射；它必须直接声明一个存在且已启用的资源码。CI 扫描执行点覆盖情况，运行时仍由后端强制校验。

## 七、关键字段

| 对象 | 第一版字段 |
| --- | --- |
| 用户 | 姓名、账号、工号、状态 |
| 角色 | 名称、编码、业务板块、状态 |
| 资源 | 名称、资源码、板块、风险、数据域、状态 |
| 菜单 | 类型、名称、编码、上级、菜单权限码、路由 |
| 组织 | 名称、上级、类型、负责人 |
| 岗位 | 名称、所属组织、正式数据策略 |

暂不增加只用于展示、可由其他字段推导或缺少真实用例的字段。

## 八、落地边界

本期不做：

- 多租户 / `tenant_id`；
- 用户级 DENY；
- 角色继承；
- 用户临时数据范围；
- API 资源层；
- REPORT / TEMPLATE 第二层资源；
- 实例 ACL、字段权限、任意 SQL / ABAC；
- 通用策略引擎。

## 九、迁移步骤

```text
盘点 lite-mall API 权限
→ 按业务动作建立资源目录
→ 形成只读旧 API 到资源码映射
→ 新旧双轨影子计算
→ 前端改用资源码
→ 后端执行点直接声明资源码
→ 分板块灰度
→ 下线旧 API 权限列表
```

最高风险是把多个旧 API 粗暴合成一个宽资源，从而让历史角色扩权；部分覆盖时应拆分资源。

## 十、验收清单

- [ ] 只有菜单与单层资源可授权；
- [ ] 用户创建无组织、岗位、数据范围字段；
- [ ] 组织页可通过用户下拉框设置组织与岗位；
- [ ] 用户授权抽屉无数据范围和临时范围入口；
- [ ] 菜单 CORE 自动反显，OPTIONAL 显眼提示；
- [ ] 高风险资源不能成为 CORE；
- [ ] 前后端统一资源码，API 不登记为资源；
- [ ] 资源与岗位数据范围同时通过才放行；
- [ ] 关键变化提升版本并写入审计；
- [ ] 不存在多租户、DENY 或第二层资源模型。

> 最终心智模型：角色做复用，直授做例外，菜单做资源包，资源码贯穿前后端，组织页设置任职，岗位决定正式数据边界。
