企业中台权限中心 · 纯静态 Demo

1. 本目录不需要数据库、业务 API 或环境变量。
2. 请将整个目录交给任意 HTTP 静态服务器，不要直接双击 index.html。
3. 页面修改保存在当前浏览器 localStorage；右上角可一键重置。
4. 本 Demo 仅用于展示。生产系统必须在可信服务端完成鉴权和数据过滤。

本地快速预览（任选其一）：

  npx serve .
  python -m http.server 4173

然后打开命令行显示的本地地址。
